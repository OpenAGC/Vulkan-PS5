#version 450
layout(local_size_x = 1) in;
layout(set = 1, binding = 3, std430) buffer Output { uint value; } output_data;
layout(push_constant) uniform Push { uint addend; } push_data;
void main() {
    output_data.value = gl_GlobalInvocationID.x + push_data.addend;
}
