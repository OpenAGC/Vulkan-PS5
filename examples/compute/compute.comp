#version 450

layout(local_size_x = 64) in;

layout(set = 0, binding = 0, std430) buffer Output {
    uint values[];
} output_buffer;

void main()
{
    uint index = gl_GlobalInvocationID.x;
    output_buffer.values[index] = index ^ 0x5a5a5a5a;
}
